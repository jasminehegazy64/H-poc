[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
httpx = ">=0.25.0"
feedparser = ">=6.0.10"
spacy = ">=3.7.0"
ollama = ">=0.3.0"
click = ">=8.1.0"
rich = ">=13.0.0"
python-dateutil = ">=2.8.0"
pytest = ">=7.4.0"

[dev-packages]

[requires]
python_version = "3.14"
